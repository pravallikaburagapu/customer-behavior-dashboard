1.average purchase frequency = DIVIDE([total orders],[total customers],0)
2.avg repeat purchase customers = DIVIDE([sales],[total customers],0)
3.avg repeat purchase customers = DIVIDE([sales],[total customers],0)
4.churn rate = DIVIDE([churned customers],[total customers],0)
5.churned customers = CALCULATE(DISTINCTCOUNT('e commerce 1'[Customer ID]),'e commerce 1'[Churn]=1)
6.Repeat purchase customers = CALCULATE(DISTINCTCOUNT('e commerce 1'[Customer ID]),FILTER(VALUES('e commerce 1'[Customer ID]),CALCULATE(COUNT('e commerce 1'[Customer ID]))>1))
7.repeat purchase rate = DIVIDE([Repeat purchase customers],[total customers],0)
8.return rate = DIVIDE([Total returns],[total orders],0)
9.sales = SUM('e commerce 2'[Total Purchase Amount])
10.total customers = DISTINCTCOUNT('e commerce 1'[Customer ID])
11.total orders = COUNTROWS('e commerce 1')
12.Total quantity = SUM('e commerce 2'[Quantity])
13.Total returns = SUM('e commerce 1'[Returns])
14.Total revenue = SUM('e commerce 2'[Total Purchase Amount])
15.total spend = SUM('e commerce 2'[Total Purchase Amount])
